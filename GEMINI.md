# 🛡️ AI Self-Verification & Quality Harness (자가 검증 철칙 하네스)

이 파일은 AI 에이전트가 작업을 수행할 때 반드시 준수해야 하는 **자체 하네싱(Self-Harnessing) 강제 규칙**입니다. 사용자가 지적하기 전에 AI 스스로 아래 검증 절차를 통과해야 합니다.

---

## 1. 모바일 & 크로스 플랫폼 퍼스트 원칙 (Mobile-First)
- **오디오/미디어 정책 (Autoplay Policy)**: 모바일 Safari/Chrome은 비동기 fetch 이후의 오디오 재생을 차단하므로, 반드시 사용자 터치(User Gesture) 시점에 `unlockAudio()`를 사전 호출할 것.
- **Web Speech API 레이스 컨디션 차단**: 음성 인식(`SpeechRecognition`)의 `onresult`와 `onend`가 겹치는 문제를 방지하기 위해 반드시 `isProcessing` 플래그로 락을 걸고, 말하는 즉시 화면에 피드백을 주는 `interimResults = true`를 기본 적용할 것.
- **마이크 권한 및 예외 처리**: `not-allowed` 등의 에러 발생 시 사용자에게 친절한 권한 허용 가이드를 즉시 안내할 것.

---

## 2. 배포 및 런타임 자체 검증 (Run & Verify First)
- **"완료되었습니다"라고 보고하기 전 필수 검증**:
  - 실제 HTTP 상태 코드(`200 OK`) 및 파일 응답을 스크립트로 직접 호출하여 확인한 뒤 보고할 것.
  - 가상/상상으로 완료되었다고 단정하지 말 것.
- **GitHub 및 외부 서비스 배포**:
  - 빈 저장소(Empty Repo)와 기존 저장소의 브랜치 유무를 API로 먼저 조회하고, 올바른 업로드/배포 경로를 사용할 것.
  - 보안 토큰 및 인증 실패 가능성을 미리 파악하여 비대화형 환경에서 멈추지 않도록 처리할 것.

---

## 3. PWA (Progressive Web App) 앱 규격 완비
- 웹앱을 배포할 때는 단순 바로가기가 아닌 '진짜 앱 설치'가 가능하도록:
  - `manifest.json` (192, 512, maskable/any 분리 아이콘, standalone 모드, 올바른 scope/id)
  - `sw.js` (서비스 워커 캐싱 및 오프라인 지원)
  - iOS 사파리 및 안드로이드 크롬 기기별 맞춤 설치 안내 가이드를 필수 탑재할 것.

---

## 4. 납품 전 자가 질의 (Self-Query Gate)
AI는 코드를 사용자에게 제출하기 전, 다음 3가지 질문을 스스로에게 던지고 모두 'YES'일 때만 납품한다:
1. *"이 코드가 모바일 실제 폰에서 터치되었을 때 권한이나 비동기 딜레이로 깨지지 않는가?"*
2. *"실제 터미널이나 네트워크에서 에러 없이 200 OK로 돌아가는 것을 내 눈으로 확인했는가?"*
3. *"사용자에게 불필요한 번거로움이나 헛걸음을 주지 않는 가장 직관적인 형태인가?"*
